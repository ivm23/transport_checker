CREATE OR REPLACE 
PACKAGE MAINCHECK AS 

    PROCEDURE STARTCHECK(RBSTABLE IN BOOLEAN, TRACKSTABLE IN BOOLEAN, SPEED IN BOOLEAN, RBSTABLENAME IN NVARCHAR2, TRACKSTABLENAME IN NVARCHAR2,DIRPATH IN NVARCHAR2, 
    RBSK IN FLOAT, EPSILON INTEGER, routeNumber in NVARCHAR2, maxStepsCount in INTEGER, maxSpeed in FLOAT, miNSpeed in FLOAT);
    PROCEDURE CreateDirForReportFiles(dirPath in NVARCHAR2);


END MAINCHECK;